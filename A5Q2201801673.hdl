CHIP A5Q2201801673{
    IN L, C, G;    
    OUT U;         

    PARTS:
    NOT(in=L, out=notL);

    AND(a=L, b=C, out=andL);

    OR(a=notL, b=C, out=orL);

    OR(a=andL, b=orL, out=or2);

    NOT(in=G, out=notG);

    AND(a=notG, b=or2, out=U);
}
