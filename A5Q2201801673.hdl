CHIP A5Q2201801673{
    IN L, C, G;    
    OUT U;         

    PARTS:
    NOT(in=L, out=notL);

    AND(a=L, b=C, out=and1);

    OR(a=notL, b=C, out=or1);

    OR(a=and1, b=or1, out=or2);

    NOT(in=G, out=notG);

    AND(a=notG, b=or2, out=U);
}
